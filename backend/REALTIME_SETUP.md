# ⚡ Realtime Scanning Mode Setup

## 🎯 Что это?

**Realtime режим** анализирует сообщения **мгновенно** при их добавлении в базу данных, вместо ожидания следующего часового запуска cron.

### 📊 Сравнение режимов:

| Режим | Как работает | Задержка | Когда использовать |
|-------|-------------|----------|-------------------|
| **CRON** (по умолчанию) | Сканирует каждый час | До 1 часа | Когда сообщения добавляются пакетами или не критична скорость |
| **REALTIME** | Анализирует сразу при добавлении | 5-10 секунд | Когда нужна мгновенная реакция на новые лиды |

---

## 🚀 Настройка Realtime режима

### Шаг 1: Включите Realtime в Supabase

1. Откройте ваш проект в [Supabase Dashboard](https://supabase.com/dashboard)
2. Перейдите в **Database** → **Replication**
3. Найдите таблицу `messages`
4. Включите **Realtime** для этой таблицы (toggle справа)
5. Нажмите **Save**

**Важно:** Supabase Realtime включен по умолчанию в новых проектах, но проверьте, что он активирован для таблицы `messages`.

### Шаг 2: Обновите .env файл

Откройте `backend/.env` и добавьте/измените:

```env
# Режим сканирования: 'cron' или 'realtime'
SCAN_MODE=realtime

# Остальные настройки остаются без изменений
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=eyJxxx...
# ...
```

### Шаг 3: Перезапустите backend

```bash
cd backend

# Остановите текущий процесс (Ctrl+C)

# Запустите заново
npm start
```

Вы должны увидеть в логах:

```
⚡ Starting REALTIME scanning mode...
✅ Realtime scanner subscribed successfully
✅ Application started successfully (REALTIME mode)
```

---

## 🎬 Как это работает?

### Процесс в Realtime режиме:

```
1. Новое сообщение добавляется в таблицу messages
   ↓
2. Supabase Realtime мгновенно отправляет уведомление
   ↓
3. Backend получает уведомление
   ↓
4. Сообщение добавляется в буфер (batch)
   ↓
5. Через 5 секунд обрабатывается весь batch
   ↓
6. Pre-filtering → AI анализ → Сохранение лидов
   ↓
7. Лиды мгновенно отправляются в Telegram
```

### Преимущества батчинга:

- **Экономия:** Если 10 сообщений приходят за 5 секунд, они обрабатываются вместе
- **Эффективность:** Меньше API вызовов к OpenRouter
- **Надежность:** Защита от перегрузки при массовом притоке сообщений

---

## 🔧 API Управление

### Получить статус сканера

```bash
curl http://localhost:3000/api/scanner/status
```

Ответ:
```json
{
  "success": true,
  "mode": "realtime",
  "status": {
    "isRunning": true,
    "pendingBatchSize": 3,
    "processedMessagesCount": 127
  }
}
```

### Запустить realtime scanner вручную

```bash
curl -X POST http://localhost:3000/api/scanner/start \
  -H "x-user-id: 00000000-0000-0000-0000-000000000001"
```

### Остановить realtime scanner

```bash
curl -X POST http://localhost:3000/api/scanner/stop \
  -H "x-user-id: 00000000-0000-0000-0000-000000000001"
```

### Запустить ручное сканирование (как cron)

```bash
curl -X POST http://localhost:3000/api/scanner/manual-scan \
  -H "x-user-id: 00000000-0000-0000-0000-000000000001"
```

---

## ⚙️ Настройка параметров

### Изменить интервал батчинга

В файле `backend/src/services/realtimeScanner.js`:

```javascript
// Изменить с 5 секунд на другое значение
const BATCH_INTERVAL = 5000; // миллисекунды

// Примеры:
// 1 секунда = 1000
// 10 секунд = 10000
// 30 секунд = 30000
```

### Отключить батчинг (обработка сразу)

```javascript
const BATCH_INTERVAL = 0; // Обработка сразу
```

⚠️ **Внимание:** Без батчинга при большом потоке сообщений увеличатся расходы на OpenRouter!

---

## 🔄 Переключение между режимами

### Переключиться на CRON режим

1. Откройте `backend/.env`
2. Измените: `SCAN_MODE=cron`
3. Перезапустите backend: `npm start`

### Переключиться на REALTIME режим

1. Откройте `backend/.env`
2. Измените: `SCAN_MODE=realtime`
3. Перезапустите backend: `npm start`

---

## 📊 Мониторинг

### Логи Realtime режима

Backend будет логировать:

```
INFO: New message received { messageId: 12345, chatName: 'Tech Chat' }
INFO: Message added to batch { messageId: 12345, batchSize: 3 }
INFO: Processing batch of new messages { count: 3 }
INFO: Lead detected and saved { userId: xxx, leadId: 42, confidence: 0.87 }
INFO: Lead posted to Telegram { userId: xxx, leadId: 42 }
```

### Проверка статуса

```bash
# Через API
curl http://localhost:3000/api/scanner/status

# Через health check
curl http://localhost:3000/health
```

---

## 🐛 Troubleshooting

### Realtime не работает

**Проблема:** Сообщения не обрабатываются

**Решения:**
1. Проверьте, что Realtime включен в Supabase для таблицы `messages`
2. Проверьте логи: `INFO: Realtime scanner subscribed successfully`
3. Убедитесь, что `SCAN_MODE=realtime` в `.env`
4. Перезапустите backend

### "CHANNEL_ERROR" в логах

**Проблема:** Ошибка подключения к Realtime

**Решения:**
1. Проверьте `SUPABASE_URL` и `SUPABASE_ANON_KEY`
2. Убедитесь, что Realtime включен в вашем Supabase плане
3. Проверьте интернет-соединение

### Большая задержка обработки

**Проблема:** Сообщения обрабатываются медленно

**Причины:**
- Слишком много активных пользователей
- OpenRouter API медленно отвечает
- Большой BATCH_INTERVAL

**Решения:**
1. Уменьшите `BATCH_INTERVAL` до 1-2 секунд
2. Проверьте скорость OpenRouter API
3. Оптимизируйте промпты пользователей

---

## 💰 Стоимость

### Realtime режим:

- ✅ **Supabase Realtime:** Включен в Free tier (до 2GB database, 500MB bandwidth)
- ✅ **OpenRouter API:** Та же стоимость, что и в CRON режиме
- ✅ **Сервер:** Никаких дополнительных затрат

### Выгода:

- **Быстрее:** Лиды приходят через секунды, а не часы
- **Эффективнее:** Батчинг снижает количество API вызовов
- **Надежнее:** Нет риска пропустить сообщения между hourly scans

---

## 🎯 Рекомендации

### Используйте REALTIME режим, если:

- ✅ Нужна быстрая реакция на новые лиды
- ✅ Сообщения добавляются постоянно, а не пакетами
- ✅ Важна мгновенная отправка в Telegram
- ✅ У вас включен Supabase Realtime

### Используйте CRON режим, если:

- ✅ Сообщения добавляются пакетами (например, раз в день)
- ✅ Задержка в 1 час приемлема
- ✅ Хотите минимизировать нагрузку на систему
- ✅ Realtime недоступен в вашем Supabase плане

---

## 📚 Дополнительно

### Документация Supabase Realtime:

- [Supabase Realtime Guide](https://supabase.com/docs/guides/realtime)
- [Postgres Changes](https://supabase.com/docs/guides/realtime/postgres-changes)

### Файлы проекта:

- `backend/src/services/realtimeScanner.js` - Realtime scanner логика
- `backend/src/api/routes/scanner.js` - API endpoints
- `backend/src/index.js` - Точка входа с выбором режима

---

## ✅ Готово!

Теперь ваша система будет анализировать сообщения **мгновенно** при их появлении в базе данных! 🚀

**Вопросы?** Проверьте логи или откройте issue.

_Последнее обновление: November 2025_



