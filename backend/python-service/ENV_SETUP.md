# 🔧 Настройка .env для Python сервиса

## Создайте файл `.env` в папке `backend/python-service/` со следующим содержимым:

```env
# ============================================
# SUPABASE CONFIGURATION
# ============================================
# URL вашего Supabase проекта
SUPABASE_URL=https://liavhyhyzqadilfmicba.supabase.co

# Anon key из Supabase (уже заполнен)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxpYXZoeWh5enFhZGlsZm1pY2JhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjE1ODQ1NzIsImV4cCI6MjA3NzE2MDU3Mn0.tlqzG7LygCEKPtFIiXxChqef4JNMaXqj69ygLww1GQM

# Database password из Supabase
# ⚠️ ВАЖНО: Замените на ваш реальный пароль базы данных!
# Найдите его в: Supabase Dashboard -> Settings -> Database -> Password
SUPABASE_DB_PASSWORD=ВАШ_ПАРОЛЬ_ОТ_БД

# ============================================
# OPENROUTER API (для Claude 3.5 Sonnet)
# ============================================
# ⚠️ НЕ НУЖЕН! Каждый пользователь использует свой API ключ из настроек!
# Python service автоматически получает openrouter_api_key из таблицы user_config
# для каждой кампании отдельно.

# ============================================
# TELEGRAM BOT (для уведомлений о горячих лидах)
# ============================================
TELEGRAM_BOT_TOKEN=ваш_токен_бота

# ============================================
# АНТИБАН НАСТРОЙКИ (можно оставить как есть)
# ============================================
# Максимальное количество сообщений в день на один аккаунт
MAX_MESSAGES_PER_DAY=25

# Минимальная задержка между сообщениями (в секундах)
MESSAGE_DELAY_MIN=30

# Максимальная задержка между сообщениями (в секундах)
MESSAGE_DELAY_MAX=120

# Задержка перед переключением на другой аккаунт (в секундах)
ACCOUNT_SWITCH_DELAY=300

# ============================================
# ЛОГИРОВАНИЕ
# ============================================
# Уровень логирования: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
```

## 📝 Что нужно заполнить:

### 1. **SUPABASE_DB_PASSWORD** (обязательно!)
   - Откройте https://supabase.com/dashboard/project/liavhyhyzqadilfmicba/settings/database
   - Скопируйте Database Password
   - Или сбросьте пароль если не помните

### 2. **OPENROUTER_API_KEY** - НЕ НУЖЕН!
   - ✅ Каждый пользователь вводит свой API ключ в настройках приложения
   - ✅ Python service автоматически получает его из БД
   - ✅ Каждая компания платит за свое AI общение

### 3. Остальные параметры можно оставить как есть

## 🚀 Быстрый запуск после настройки:

```bash
cd backend/python-service

# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Запустить сервис
python main.py
```

## ✅ Проверка работы:

Сервис должен вывести:
```
🤖 AI Messaging Service
============================================
Started at: 2025-XX-XX XX:XX:XX UTC

🔧 Initializing components...
✅ Connected to Supabase database
✅ All components initialized
```

## ❗ Частые ошибки:

### "Could not connect to database"
- Проверьте `SUPABASE_DB_PASSWORD`
- Убедитесь что проект активен в Supabase

### "OpenRouter API error 401"
- Пользователь должен ввести свой API ключ в настройках приложения
- Проверьте что ключ пользователя валидный
- Убедитесь что у пользователя есть баланс на OpenRouter

### "ModuleNotFoundError"
- Убедитесь что установили зависимости: `pip install -r requirements.txt`
- Убедитесь что активировали venv

