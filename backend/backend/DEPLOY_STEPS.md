# 🚀 Пошаговая инструкция по деплою на Timeweb Cloud

## ✅ Что уже готово:

- ✅ Код подготовлен для деплоя
- ✅ Dockerfile для backend готов
- ✅ Dockerfile для frontend готов  
- ✅ Supabase Auth интегрирован
- ✅ Все зависимости установлены

---

## 📋 ШАГ 1: Подготовка GitHub репозитория

### Если репозиторий еще не создан:

1. Зайдите на **GitHub.com**
2. Создайте новый репозиторий: `telegram-lead-scanner`
3. Скопируйте URL репозитория

### Обновите remote (если нужно):

```bash
cd "C:\Users\wemd1\Desktop\new ai"
git remote set-url origin https://github.com/YOUR-USERNAME/telegram-lead-scanner.git
git push -u origin main
```

---

## 🔧 ШАГ 2: Деплой Backend на Timeweb

### 2.1 Создать Backend приложение

1. Зайдите в **Timeweb Cloud Dashboard**: https://timeweb.cloud
2. Перейдите в **"Облачные серверы"** → **"Приложения"**
3. Нажмите **"Создать приложение"**
4. Выберите:
   - **Тип**: Backend
   - **Фреймворк**: Docker
   - **Название**: `telegram-lead-scanner-backend`

### 2.2 Настроить VCS (GitHub)

1. В настройках приложения найдите **"VCS"** или **"Репозиторий"**
2. Подключите GitHub:
   - Выберите **GitHub** как провайдер
   - Авторизуйтесь через GitHub
   - Выберите ваш репозиторий: `telegram-lead-scanner`
   - Ветка: `main`

### 2.3 Настроить Dockerfile

1. В настройках сборки укажите:
   - **Dockerfile путь**: `Dockerfile` (из корня репозитория)
   - **Контекст сборки**: `/` (корень)
   - **Порт**: `3000`

### 2.4 Настроить переменные окружения

В разделе **"Переменные окружения"** добавьте:

```env
# Supabase
SUPABASE_URL=https://liavhyhyzqadilfmicba.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-key

# Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Server
PORT=3000
NODE_ENV=production

# OpenRouter (опционально, можно настроить через UI)
OPENROUTER_API_KEY=your-openrouter-key
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
AI_MODEL=google/gemini-2.0-flash-001
```

### 2.5 Задеплоить

1. Нажмите **"Создать"** или **"Деплой"**
2. Дождитесь завершения сборки (обычно 3-5 минут)
3. Проверьте логи - не должно быть ошибок

### 2.6 Проверить работу

Откройте в браузере:
```
https://wemdio-parserandscanner-40d8.twc1.net/health
```

Должен вернуть:
```json
{
  "status": "healthy",
  "database": "healthy",
  ...
}
```

---

## 🎨 ШАГ 3: Деплой Frontend на Timeweb

### 3.1 Создать Frontend приложение

1. В **Timeweb Cloud Dashboard** → **"Приложения"**
2. Нажмите **"Создать приложение"**
3. Выберите:
   - **Тип**: Frontend
   - **Фреймворк**: Docker
   - **Название**: `telegram-lead-scanner-frontend`

### 3.2 Настроить VCS (GitHub)

1. Подключите тот же GitHub репозиторий
2. Ветка: `main`

### 3.3 Настроить Dockerfile

1. В настройках сборки укажите:
   - **Dockerfile путь**: `frontend/Dockerfile`
   - **Контекст сборки**: `/` (корень)
   - **Порт**: `80`

### 3.4 Настроить переменные окружения (опционально)

Если хотите переопределить Supabase URL:

```env
VITE_SUPABASE_URL=https://liavhyhyzqadilfmicba.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
VITE_API_URL=https://wemdio-parserandscanner-40d8.twc1.net/api
```

**Важно:** Эти переменные должны быть установлены **ДО** первой сборки!

### 3.5 Задеплоить

1. Нажмите **"Создать"** или **"Деплой"**
2. Дождитесь завершения сборки
3. Проверьте логи

### 3.6 Проверить работу

Откройте frontend URL в браузере:
- Должна показаться **страница входа**
- Попробуйте войти с тестовым аккаунтом

---

## 🔐 ШАГ 4: Настройка Supabase Auth

### 4.1 Отключить публичную регистрацию

1. Зайдите в **Supabase Dashboard**: https://supabase.com/dashboard
2. Выберите проект: `liavhyhyzqadilfmicba`
3. **Authentication** → **Providers** → **Email**
4. **Отключите** "Enable sign ups"
5. Нажмите **Save**

### 4.2 Создать тестовый аккаунт

1. **Authentication** → **Users** → **Add user**
2. Введите:
   - Email: `test@example.com`
   - Password: `TestPass123!`
   - ✅ **Auto Confirm User**
3. Нажмите **Create user**

### 4.3 Проверить вход

1. Откройте frontend URL
2. Войдите с созданным аккаунтом
3. Должно работать! ✅

---

## 🐛 Решение проблем

### Проблема: Backend возвращает 502

**Решение:**
1. Проверьте логи в Timeweb Dashboard
2. Убедитесь что `PORT=3000` в переменных окружения
3. Проверьте что Dockerfile правильный

### Проблема: Frontend не может подключиться к Backend

**Решение:**
1. Проверьте что backend URL правильный в `frontend/src/services/api.js`
2. Убедитесь что CORS настроен в backend (уже настроен)
3. Проверьте что backend работает: `/health` endpoint

### Проблема: Ошибка сборки frontend

**Решение:**
1. Проверьте логи сборки
2. Убедитесь что `@supabase/supabase-js` в `package.json`
3. Проверьте что Dockerfile правильный

### Проблема: "Repository not found"

**Решение:**
1. Создайте репозиторий на GitHub
2. Обновите remote URL:
   ```bash
   git remote set-url origin https://github.com/YOUR-USERNAME/REPO-NAME.git
   git push -u origin main
   ```

---

## ✅ Чек-лист перед деплоем

- [ ] Код закоммичен и запушен в GitHub
- [ ] GitHub репозиторий создан и доступен
- [ ] Backend переменные окружения готовы
- [ ] Frontend переменные окружения готовы (если нужны)
- [ ] Supabase публичная регистрация отключена
- [ ] Тестовый аккаунт создан в Supabase
- [ ] Telegram Bot Token валидный
- [ ] Backend URL правильный в frontend коде

---

## 🎯 После деплоя

1. **Проверьте backend**: `https://wemdio-parserandscanner-40d8.twc1.net/health`
2. **Проверьте frontend**: откройте frontend URL
3. **Войдите** с тестовым аккаунтом
4. **Настройте конфигурацию** в UI
5. **Запустите сканер** через UI

---

## 🚀 Готово!

После выполнения всех шагов у вас будет:
- ✅ Backend работающий на Timeweb
- ✅ Frontend работающий на Timeweb
- ✅ Защищенное приложение с Supabase Auth
- ✅ Автоматический деплой при каждом push

Удачи! 🎉

