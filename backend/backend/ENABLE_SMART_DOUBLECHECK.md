# 💰 Как включить экономию на AI анализе (Smart Double-Check)

## Что это даёт?
- **Экономия 50-70% на Gemini 3 Pro** (самая дорогая часть анализа)
- **Качество не страдает**: двойная проверка остаётся для "сомнительных" лидов
- **Безопасно**: "очевидные" лиды с высокой уверенностью DeepSeek не требуют дорогой перепроверки

---

## 📋 Инструкция (Timeweb Cloud)

### Шаг 1: Открыть настройки приложения

1. Зайти в **Timeweb Cloud** → **Приложения**
2. Выбрать ваше приложение (backend)
3. Перейти в раздел **"Переменные окружения"** (Environment Variables)

### Шаг 2: Добавить две новые переменные

Добавьте (или обновите, если уже есть):

```
DOUBLECHECK_MODE=smart
DOUBLECHECK_MIN_CONFIDENCE=90
```

**Как добавить:**
- Нажмите кнопку **"Добавить переменную"**
- В поле "Имя" введите: `DOUBLECHECK_MODE`
- В поле "Значение" введите: `smart`
- Нажмите "Сохранить"
- Повторите для второй переменной

### Шаг 3: Перезапустить контейнер

1. В Timeweb Dashboard найдите кнопку **"Перезапустить"** (или "Restart")
2. Подождите 30-60 секунд, пока контейнер перезапустится
3. Проверьте логи — должно появиться:
   ```
   [info] Double-check mode: smart (min confidence: 90)
   ```

---

## ⚙️ Настройка (опционально)

### Изменить порог уверенности

Если хотите **больше экономить** (но с риском пропуска FP):

```
DOUBLECHECK_MIN_CONFIDENCE=85
```

**Рекомендации:**
- `90` (рекомендуется) — безопасно, экономия ~50%
- `85` — агрессивно, экономия ~70%, но больше риск пропуска "мусорных" лидов
- `95` — консервативно, экономия ~30%, минимальный риск

### Вернуться к старому режиму (всегда проверять)

```
DOUBLECHECK_MODE=always
```

Или просто удалите обе переменные из Timeweb.

---

## 🔍 Как это работает?

### Режим `DOUBLECHECK_MODE=smart`

Gemini 3 Pro (дорогая модель) вызывается **только** если:
1. DeepSeek дал **низкую уверенность** (< 90%)
2. Сообщение **короткое** (< 30 символов)
3. В сообщении есть **подозрительные слова** (вакансия, предлагаю, услуги, реклама)
4. **Нет matched_criteria** (DeepSeek не смог объяснить, почему это лид)

Во всех остальных случаях — доверяем DeepSeek.

### Режим `DOUBLECHECK_MODE=always` (по умолчанию)

Gemini 3 Pro проверяет **каждый** найденный лид.

---

## 📊 Мониторинг экономии

После включения можно проверить логи backend:

```
[info] AI analysis complete (batch) {"isMatch":true,"confidence":92,"validationPassed":true,"meetsThreshold":true}
[info] Skipping Gemini double-check (high confidence) {"messageId":12345,"confidence":92,"threshold":90}
[info] Saved detected lead {"leadId":678,"confidence":92}
```

Строка `Skipping Gemini double-check` = вы сэкономили $0.002-0.005 на этом лиде.

---

## ❓ FAQ

**Q: Это безопасно?**  
A: Да, если `DOUBLECHECK_MIN_CONFIDENCE=90`. Все лиды с уверенностью < 90% всё равно проходят двойную проверку.

**Q: Сколько я сэкономлю?**  
A: Зависит от ваших критериев. Обычно 50-70% лидов имеют высокую уверенность и не нуждаются в Gemini.

**Q: Качество анализа упадёт?**  
A: Нет, если порог 90+. DeepSeek 3.2 очень точен при высокой уверенности.

**Q: Можно ли включить только для аудита, а не для realtime?**  
A: Сейчас настройка глобальная. Если нужна гибкость — напишите в чат.

---

## 🚀 Результат

- **До:** каждый лид = 2 API вызова (DeepSeek + Gemini)
- **После:** ~50-70% лидов = 1 API вызов (только DeepSeek)
- **Экономия:** ~$0.10-0.30 на каждые 100 лидов (зависит от длины сообщений)

