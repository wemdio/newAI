# 🔐 Настройка закрытой регистрации (Invite-Only)

## 📋 Что было сделано

✅ Создана страница входа с Supabase Auth  
✅ Приложение защищено - без входа ничего не работает  
✅ User ID теперь берется из Supabase Auth (безопасно)  
✅ Добавлена кнопка выхода  

---

## ⚙️ Настройка Supabase Dashboard

### Шаг 1: Отключить публичную регистрацию

1. **Откройте Supabase Dashboard** → https://supabase.com/dashboard
2. Выберите ваш проект: **liavhyhyzqadilfmicba**
3. Перейдите в **Authentication** → **Providers** (в левом меню)
4. Найдите **Email** провайдер
5. **ОТКЛЮЧИТЕ** опцию **"Enable sign ups"** (Confirm email)
   - Переключите чекбокс в положение OFF
   - Нажмите **Save**

**Теперь никто не сможет зарегистрироваться самостоятельно!** 🎉

---

## 👥 Как создавать аккаунты для клиентов

### Вариант 1: Через Dashboard (рекомендуется)

1. **Authentication** → **Users** (левое меню)
2. Нажмите **"Invite user"** или **"Add user"**
3. Введите:
   - **Email клиента** (например: `client@company.com`)
   - **Временный пароль** (необязательно - можно оставить пустым)
   - **Auto-confirm user**: ✅ **Включите** (чтобы не требовалось подтверждение email)
4. Нажмите **"Create user"** или **"Send invitation"**

**Что произойдет:**
- Клиент получит письмо с приглашением (если выбрали "Invite")
- Клиент сможет зайти сразу (если выбрали "Add user" с паролем)
- Его email будет в списке пользователей

---

### Вариант 2: Через SQL (для массового создания)

Если нужно создать много клиентов сразу:

1. **SQL Editor** (левое меню в Supabase)
2. Вставьте и выполните:

```sql
-- Создать нового пользователя
SELECT auth.admin_create_user(
  email := 'client@example.com',
  password := 'TemporaryPassword123!',
  email_confirm := true
);
```

Для нескольких клиентов сразу:

```sql
-- Создать 3 пользователей
SELECT auth.admin_create_user(
  email := 'client1@company.com',
  password := 'Password123!',
  email_confirm := true
);

SELECT auth.admin_create_user(
  email := 'client2@company.com',
  password := 'Password123!',
  email_confirm := true
);

SELECT auth.admin_create_user(
  email := 'client3@company.com',
  password := 'Password123!',
  email_confirm := true
);
```

---

## 📧 Что сообщать клиенту

После создания аккаунта, отправьте клиенту:

```
Привет!

Ваш доступ к Telegram Lead Scanner готов:

🔗 Ссылка: https://your-app-url.com
📧 Email: client@company.com
🔑 Пароль: TemporaryPassword123!

При первом входе рекомендую сменить пароль в настройках.

С уважением,
Команда Lead Scanner
```

---

## 🔄 Сброс пароля клиента

Если клиент забыл пароль:

1. **Authentication** → **Users**
2. Найдите пользователя в списке
3. Нажмите **"..."** (три точки) → **"Send password recovery"**
4. Клиент получит письмо со ссылкой для сброса пароля

---

## 🗑️ Удаление клиента

Если клиент не оплатил или нужно отключить доступ:

1. **Authentication** → **Users**
2. Найдите пользователя
3. Нажмите **"..."** → **"Delete user"**

**Важно:** Это удалит пользователя, но не его данные (leads, config).  
Для полной очистки нужно также удалить записи из таблиц:
- `user_config`
- `detected_leads`

SQL для полной очистки:

```sql
-- Заменить 'user-id-here' на реальный UUID пользователя
DELETE FROM detected_leads WHERE user_id = 'user-id-here';
DELETE FROM user_config WHERE user_id = 'user-id-here';
```

---

## 👨‍💼 Ваш супер-админ аккаунт

Создайте себе аккаунт для управления:

1. **Authentication** → **Users** → **Add user**
2. Введите **ваш личный email**
3. Установите **надежный пароль**
4. **Auto-confirm**: ✅

Этот аккаунт используйте для:
- Проверки работы системы
- Тестирования
- Доступа к настройкам

---

## 📊 Проверка активных пользователей

Посмотреть всех пользователей:

1. **Authentication** → **Users**
2. Здесь видны все аккаунты:
   - Email
   - Дата создания
   - Последний вход
   - Статус (Confirmed/Unconfirmed)

Или через SQL:

```sql
-- Список всех пользователей
SELECT 
  id,
  email,
  created_at,
  last_sign_in_at,
  email_confirmed_at
FROM auth.users
ORDER BY created_at DESC;
```

---

## 🚀 Установка и запуск

### 1. Установить зависимости

```bash
cd frontend
npm install
```

### 2. Создать .env файл (опционально)

Если хотите переопределить Supabase URL/ключи:

```env
VITE_SUPABASE_URL=https://liavhyhyzqadilfmicba.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

*(Если не создавать .env, будут использоваться значения по умолчанию)*

### 3. Запустить frontend

```bash
npm run dev
```

Откройте: http://localhost:5173

---

## ✅ Проверка работы

1. **Откройте приложение** → должна показаться страница входа
2. **Попробуйте войти** с несуществующим email → должна быть ошибка
3. **Создайте тестового пользователя** в Supabase
4. **Войдите** с созданным email/паролем → должно пропустить в приложение
5. **Проверьте** что email отображается в шапке
6. **Нажмите "Выйти"** → должно вернуть на страницу входа

---

## 🔒 Безопасность

### Что защищено:

✅ Невозможно зарегистрироваться без приглашения  
✅ API получает реальный User ID из Supabase Auth  
✅ Сессия хранится безопасно (HTTP-only cookies через Supabase)  
✅ Автоматический выход при истечении токена  

### Что еще можно улучшить:

- **Email верификация** (если важно проверять почту)
- **2FA** (двухфакторная аутентификация)
- **Роли** (admin, user, viewer)
- **Rate limiting** на вход (защита от брутфорса)

---

## 🆘 Частые вопросы

### Q: Клиент не получил письмо с приглашением
**A:** Проверьте:
1. Spam папку
2. Настройки SMTP в Supabase (Authentication → Email Templates)
3. Используйте "Add user" вместо "Invite" и отправьте пароль вручную

### Q: Ошибка "Invalid login credentials"
**A:** 
- Проверьте правильность email/пароля
- Убедитесь что пользователь создан (Authentication → Users)
- Проверьте что "Auto-confirm" был включен

### Q: Как сменить пароль пользователю?
**A:** 
1. Authentication → Users → найти пользователя
2. Три точки → "Send password recovery"
3. Или установить новый пароль через SQL:

```sql
-- Установить новый пароль пользователю
UPDATE auth.users 
SET 
  encrypted_password = crypt('NewPassword123!', gen_salt('bf')),
  updated_at = now()
WHERE email = 'client@example.com';
```

### Q: Можно ли ограничить количество аккаунтов?
**A:** Да! Supabase Free tier:
- До 50,000 активных пользователей
- Неограниченное количество регистраций (но у вас они отключены)

---

## 📈 Мониторинг использования

Отследить кто заходил:

```sql
-- Активность пользователей за последние 7 дней
SELECT 
  email,
  last_sign_in_at,
  sign_in_count
FROM auth.users
WHERE last_sign_in_at > now() - interval '7 days'
ORDER BY last_sign_in_at DESC;
```

---

## 🎯 Готово!

Теперь у вас **полностью защищенное приложение** с контролируемым доступом.

Только те, кого вы добавите вручную, смогут войти! 🔐

**Следующие шаги:**
1. Отключите публичную регистрацию в Supabase (см. выше)
2. Создайте себе аккаунт
3. Протестируйте вход
4. Создайте аккаунты для платящих клиентов

Удачи! 🚀

