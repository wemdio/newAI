# 🚀 Быстрый старт: Telegram Mini App Bot

## Проблема
Menu Button не передает данные пользователя (`initData` пустой), поэтому автоматический вход через Telegram не работает.

## ✅ Решение
Используйте бота с Web App кнопкой вместо Menu Button.

---

## 📋 Шаги для запуска

### 1️⃣ Убедитесь что токен бота установлен

В Timeweb Cloud или локально в `.env`:
```env
TELEGRAM_MINI_APP_BOT_TOKEN=ваш_токен_от_botfather
```

### 2️⃣ Бот запускается АВТОМАТИЧЕСКИ! ✅

**Бот теперь запускается вместе с основным приложением!**

При запуске backend вы увидите в логах:
```
🤖 Starting Mini App bot...
✅ Mini App Bot started
🤖 Mini App bot is ready. Users can send /start to get Web App button.
```

**Ничего дополнительно делать не нужно!** 🎉

Бот работает **24/7** вместе с вашим приложением.

### 3️⃣ Проверьте что бот работает

Просто перезапустите приложение в Timeweb Cloud (или локально `npm start`).

В логах должна быть строка:
```
✅ Mini App bot started (if token provided)
```

### 4️⃣ Откройте бота в Telegram

1. Найдите вашего бота (например `@polza_leads_bot`)
2. Отправьте `/start`
3. Нажмите кнопку **"🚀 Открыть Lead Scanner"**

✅ **Mini App откроется с автоматическим входом через Telegram!**

---

## 🔧 Как это работает

### Старый способ (Menu Button) ❌
```
User → Menu Button → Mini App открывается
❌ initData пустой → нет автоматического входа
```

### Новый способ (Web App кнопка) ✅
```
User → /start → Bot отправляет inline кнопку → User нажимает
✅ initData передается → автоматический вход работает!
```

---

## 🎯 Что делает бот?

**Файл:** `backend/telegram-bot-mini-app.js`

```javascript
// При /start отправляет сообщение с Web App кнопкой
bot.onText(/\/start/, (msg) => {
  bot.sendMessage(chatId, 'Привет! Нажмите кнопку ниже...', {
    reply_markup: {
      inline_keyboard: [[
        {
          text: '🚀 Открыть Lead Scanner',
          web_app: { url: 'https://telegram-scanner.ru' }
        }
      ]]
    }
  });
});
```

**Важно:** `web_app` кнопка автоматически передает `initData` в Mini App!

---

## 🆘 Troubleshooting

### Бот не отвечает
- Проверьте что `TELEGRAM_MINI_APP_BOT_TOKEN` правильный
- Проверьте что процесс запущен (`npm run miniapp-bot`)
- Проверьте логи

### Mini App не открывается
- Убедитесь что URL правильный: `https://telegram-scanner.ru`
- Проверьте что домен доступен
- Попробуйте открыть URL в браузере

### Все еще показывает форму логина
Это значит `initData` все еще пустой. Проверьте:

1. **Открываете через правильную кнопку?**
   - ✅ Через inline кнопку от бота (после /start)
   - ❌ Через Menu Button

2. **Проверьте логи в консоли** (если можете):
   ```javascript
   console.log('initData:', tg.initData);
   console.log('user:', tg.initDataUnsafe.user);
   ```

---

## 💡 Альтернатива: оставить email/password

Если бот слишком сложно настроить, можно **оставить текущий вариант**:
- В браузере: email/password
- В Telegram через Menu Button: email/password

Пользователи смогут использовать Mini App, просто с обычным входом.

---

## ✅ Рекомендация

Для **production** используйте **два способа**:
1. Web App кнопка (для новых пользователей) - автоматический вход
2. Email/password (для существующих пользователей) - если открывают через Menu Button

Текущая реализация уже поддерживает оба варианта! 🎉

