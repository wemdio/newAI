# 🚀 Setup & Run Guide

## ✅ Что уже готово

- ✅ Backend полностью настроен
- ✅ Frontend создан и готов
- ✅ Telegram Bot Token добавлен
- ✅ Все зависимости установлены

## 📝 Что нужно сделать

### 1️⃣ Получите Supabase Credentials

**Вам нужны две вещи:**

1. **SUPABASE_URL** - URL вашего проекта
2. **SUPABASE_ANON_KEY** - Anon (public) ключ

**Где найти:**

1. Откройте ваш проект на [supabase.com](https://supabase.com)
2. Перейдите в **Settings** → **API**
3. Скопируйте:
   - **Project URL** → это ваш `SUPABASE_URL`
   - **anon public** key → это ваш `SUPABASE_ANON_KEY`

### 2️⃣ Обновите backend/.env файл

Откройте файл `backend/.env` и замените:

```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key-here
```

На ваши реальные credentials:

```env
SUPABASE_URL=https://xxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxx...
```

### 3️⃣ Запустите миграции базы данных

1. Откройте [SQL Editor в Supabase](https://supabase.com/dashboard/project/_/sql)
2. Выполните миграции по порядку:
   - `backend/src/database/migrations/001_create_user_config.sql`
   - `backend/src/database/migrations/002_create_detected_leads.sql`
   - `backend/src/database/migrations/003_create_api_usage.sql`
   - `backend/src/database/migrations/004_create_processing_logs.sql`

**Важно:** Запускайте каждую миграцию отдельно!

### 4️⃣ Проверьте таблицу messages

Убедитесь что в вашей базе данных есть таблица `messages` с полями:
- `id` (int8)
- `message_time` (timestamptz)
- `chat_name` (text)
- `first_name` (text)
- `last_name` (text)
- `username` (text)
- `bio` (text)
- `message` (text)
- `user_id` (int8)
- `profile_link` (text)

## 🎯 Запуск приложения

### Терминал 1 - Backend

```bash
cd backend
npm run dev
```

Должно появиться:
```
🚀 Server running on port 3000
📊 Health check: http://localhost:3000/health
```

### Терминал 2 - Frontend

```bash
cd frontend
npm run dev
```

Должно появиться:
```
  ➜  Local:   http://localhost:5173/
```

## 🌐 Откройте приложение

Откройте браузер и перейдите на: **http://localhost:5173**

## ⚙️ Настройка системы

1. Откройте **Configuration** (⚙️)
2. Заполните форму:
   - **OpenRouter API Key** - получите на [openrouter.ai/keys](https://openrouter.ai/keys)
   - **Telegram Channel ID** - ваш приватный канал (начинается с `-100`)
   - **Lead Detection Criteria** - опишите кого вы ищете
3. Нажмите **Save Configuration**
4. Нажмите **Test Prompt** чтобы проверить

## 🧪 Тестирование

### Проверка Backend

```bash
cd backend

# Тест подключения к БД
npm run test-db

# Ручной запуск сканирования
npm run test-scan
```

### Проверка через API

```bash
# Health check
curl http://localhost:3000/health

# Получить конфигурацию
curl http://localhost:3000/api/config \
  -H "x-user-id: 00000000-0000-0000-0000-000000000001"
```

## 🎉 Готово!

Система теперь работает! 

**Что происходит автоматически:**
- ✅ Каждый час система сканирует новые сообщения
- ✅ Находит лиды с помощью AI
- ✅ Постит их в Telegram канал
- ✅ Отслеживает затраты

**Вы можете:**
- 📊 Смотреть статистику на Dashboard
- 🎯 Управлять лидами на странице Leads
- ⚙️ Изменять настройки в Configuration

## 🐛 Troubleshooting

### Backend не запускается

**Ошибка:** "Cannot connect to database"
- Проверьте `SUPABASE_URL` и `SUPABASE_ANON_KEY` в `backend/.env`
- Убедитесь что миграции выполнены

**Ошибка:** "Telegram bot token invalid"
- Проверьте `TELEGRAM_BOT_TOKEN` в `backend/.env`

### Frontend показывает ошибки

**Ошибка:** "Failed to load config"
- Убедитесь что backend запущен на порту 3000
- Проверьте что создана конфигурация пользователя

**Ошибка:** "CORS error"
- Проверьте что в `backend/.env` установлен `FRONTEND_URL=http://localhost:5173`

### Лиды не находятся

- Убедитесь что в таблице `messages` есть сообщения
- Проверьте что `is_active = true` в конфигурации
- Дождитесь начала следующего часа для автоматического сканирования
- Или запустите вручную: `npm run test-scan` в папке backend

## 📞 Поддержка

Если что-то не работает:

1. Проверьте логи backend: смотрите в терминал где запущен `npm run dev`
2. Проверьте `backend/logs/` для детальных логов
3. Откройте DevTools в браузере (F12) для ошибок frontend

## 🎯 Следующие шаги

1. **Добавьте сообщения в таблицу `messages`** (если их нет)
2. **Настройте OpenRouter API key** (нужен баланс ~$1)
3. **Создайте Telegram канал** и добавьте бота админом
4. **Настройте Lead Criteria** - опишите кого ищете
5. **Запустите тестовое сканирование**
6. **Дождитесь первых лидов!** 🎉

---

**Система полностью готова к работе!** 🚀

_Последнее обновление: November 2025_

