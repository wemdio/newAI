# 🤖 AI Messaging Feature

Автоматический AI-powered сервис для общения с найденными лидами через Telegram.

## 📋 Что добавлено

### 1. База данных (4 новые таблицы)
- `telegram_accounts` - Telegram аккаунты для рассылки
- `messaging_campaigns` - Кампании с промптами и критериями
- `ai_conversations` - История диалогов с лидами
- `hot_leads` - Горячие лиды с полной историей

### 2. Python сервис (Telethon + AI)
Расположение: `backend/python-service/`

**Модули:**
- `main.py` - Главный цикл обработки
- `telethon_client.py` - Telegram клиент (Telethon)
- `ai_communicator.py` - AI общение (Claude 3.5 Sonnet)
- `safety_manager.py` - Антибан система
- `lead_manager.py` - Управление лидами
- `supabase_client.py` - Подключение к БД
- `tdata_converter.py` - Конвертер tdata -> session

**Возможности:**
- ✅ Подключение до 5+ Telegram аккаунтов
- ✅ Умная ротация аккаунтов
- ✅ Лимиты (25 сообщений/день на аккаунт)
- ✅ Случайные задержки (30-120 сек)
- ✅ Поддержка прокси (SOCKS5/HTTP)
- ✅ Обработка FloodWait и банов
- ✅ AI-диалоги через Claude 3.5 Sonnet
- ✅ Детекция горячих лидов

### 3. Backend API
Новые endpoints в `/api/messaging`:

**Аккаунты:**
- `GET /api/messaging/accounts` - Список аккаунтов
- `POST /api/messaging/accounts` - Добавить аккаунт
- `PUT /api/messaging/accounts/:id` - Обновить
- `DELETE /api/messaging/accounts/:id` - Удалить

**Кампании:**
- `GET /api/messaging/campaigns` - Список кампаний
- `POST /api/messaging/campaigns` - Создать кампанию
- `PUT /api/messaging/campaigns/:id` - Обновить
- `POST /api/messaging/campaigns/:id/start` - Запустить
- `POST /api/messaging/campaigns/:id/pause` - Приостановить
- `POST /api/messaging/campaigns/:id/stop` - Остановить

**Диалоги и лиды:**
- `GET /api/messaging/conversations` - Список диалогов
- `GET /api/messaging/conversations/:id` - Детали диалога
- `GET /api/messaging/hot-leads` - Горячие лиды
- `GET /api/messaging/stats` - Статистика

### 4. Frontend UI
Новая страница: `frontend/src/pages/AIMessaging.jsx`

**Секции:**
1. **📊 Статистика** - Обзор кампаний, аккаунтов, диалогов
2. **📱 Аккаунты** - Управление Telegram аккаунтами
3. **🎯 Кампании** - Создание и управление кампаниями
4. **💬 Диалоги** - Мониторинг активных разговоров
5. **🔥 Горячие лиды** - Лиды готовые к передаче менеджеру

## 🚀 Установка и настройка

### Шаг 1: Миграции базы данных

Применить миграции в Supabase:
```sql
-- Запустить файлы по порядку:
backend/src/database/migrations/007_create_telegram_accounts.sql
backend/src/database/migrations/008_create_messaging_campaigns.sql
backend/src/database/migrations/009_create_ai_conversations.sql
backend/src/database/migrations/010_create_hot_leads.sql
```

### Шаг 2: Python сервис

```bash
cd backend/python-service

# Создать виртуальное окружение
python -m venv venv

# Активировать
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Настроить .env
cp .env.example .env
# Заполнить:
# - SUPABASE_URL
# - SUPABASE_ANON_KEY
# - SUPABASE_DB_PASSWORD
# - OPENROUTER_API_KEY
```

### Шаг 3: Запуск

```bash
# Terminal 1: Backend (Node.js)
cd backend
npm run dev

# Terminal 2: Frontend
cd frontend
npm run dev

# Terminal 3: Python сервис
cd backend/python-service
python main.py
```

## 📖 Как использовать

### 1. Добавить Telegram аккаунты

**Опция A: Через session файл (рекомендуется для начала)**
1. Создайте session через Telethon:
```python
from telethon import TelegramClient
client = TelegramClient('my_session', api_id, api_hash)
await client.start(phone='+1234567890')
```
2. Разместите `my_session.session` в `backend/python-service/sessions/`
3. Добавьте аккаунт через UI (указав api_id и api_hash)

**Опция B: Через tdata (будущая функция)**
- Загрузка tdata folder через UI
- Автоматическая конвертация в session

### 2. Создать кампанию

1. Перейти на страницу "AI Рассылки"
2. Нажать "+ Создать кампанию"
3. Заполнить:
   - **Название**: Например "Весенняя рассылка 2025"
   - **Промпт общения**: Как AI должен общаться
   ```
   Ты менеджер компании X. Веди диалог естественно и по-человечески.
   Узнай потребности клиента. Предложи помощь. Не будь навязчивым.
   ```
   - **Критерии горячего лида**: Когда считать лида горячим
   ```
   Лид горячий если:
   - Спросил о цене или бюджете
   - Хочет встречу или демо
   - Спрашивает про сроки
   - Готов обсудить детали
   ```
   - **Telegram канал**: ID канала для уведомлений о горячих лидах

4. Нажать "Создать"

### 3. Запустить кампанию

1. В списке кампаний нажать "Запустить"
2. Python сервис начнет обработку:
   - Получит необработанные лиды (`detected_leads WHERE is_contacted=false`)
   - Выберет доступный аккаунт
   - Сгенерирует первое сообщение через AI
   - Отправит через Telethon
   - Будет слушать ответы и продолжать диалог

### 4. Мониторинг

- **Статистика** (верх страницы): Общие цифры
- **Диалоги**: Активные разговоры в реальном времени
- **Горячие лиды**: Лиды готовые к передаче менеджеру

## 🛡️ Антибан система

### Лимиты
- **25 сообщений/день** на аккаунт
- **30-120 секунд** задержка между сообщениями (случайно)
- **5 минут** между переключениями аккаунтов

### Обработка ошибок
- **FloodWait**: Автоматическая пауза аккаунта на указанное время
- **Ban**: Автоматическая пометка аккаунта как забаненного
- **Прокси**: Автоматическое использование прокси из конфига

### Ротация
- Аккаунты используются по очереди
- Приоритет отдается давно не использованным
- Аккаунты с исчерпанным лимитом пропускаются

## 🎯 Детекция горячих лидов

AI анализирует каждый ответ лида согласно критериям из кампании.

**При детекции горячего лида:**
1. Статус диалога → `hot_lead`
2. Создается запись в `hot_leads`
3. Уведомление в Telegram канал (если настроено)
4. Отображается в разделе "🔥 Горячие лиды"

**Пример критериев:**
```
Лид горячий если:
- Упоминает конкретный бюджет
- Просит встречу/звонок/демо
- Спрашивает про сроки запуска
- Готов обсудить детали проекта
- Запрашивает коммерческое предложение
```

## 📊 Архитектура

```
┌─────────────────────────────────────────────────┐
│              Frontend (React)                   │
│  - Управление аккаунтами                        │
│  - Создание кампаний                            │
│  - Мониторинг диалогов                          │
│  - Просмотр горячих лидов                       │
└────────────┬────────────────────────────────────┘
             │ HTTP API
┌────────────▼────────────────────────────────────┐
│          Backend (Node.js)                      │
│  - REST API /api/messaging/*                    │
│  - Управление данными через Supabase            │
└────────────┬────────────────────────────────────┘
             │ PostgreSQL
┌────────────▼────────────────────────────────────┐
│            Supabase (PostgreSQL)                │
│  - telegram_accounts                            │
│  - messaging_campaigns                          │
│  - ai_conversations                             │
│  - hot_leads                                    │
│  - detected_leads (существующая)                │
└────────────┬────────────────────────────────────┘
             │ SQL Queries
┌────────────▼────────────────────────────────────┐
│        Python Service (Telethon + AI)           │
│  - Читает кампании и лиды из БД                 │
│  - Подключается к Telegram (Telethon)           │
│  - Генерирует ответы через Claude 3.5           │
│  - Применяет антибан логику                     │
│  - Сохраняет результаты в БД                    │
└─────────────────────────────────────────────────┘
```

## ⚠️ Важные замечания

### Безопасность
- ✅ Session файлы храните в безопасности
- ✅ Не коммитьте `.env` файлы
- ✅ Используйте прокси для каждого аккаунта
- ✅ Соблюдайте лимиты Telegram

### Ограничения Telegram
- **Лимит сообщений**: ~20-30/день безопасно
- **FloodWait**: Telegram может ограничить на 1-24 часа
- **Бан риск**: Новые аккаунты более уязвимы
- **Прогрев**: Новые аккаунты лучше "прогреть" перед рассылкой

### Рекомендации
1. Начните с 1-2 аккаунтов
2. Используйте прокси для каждого
3. Установите консервативные лимиты (20/день)
4. Мониторьте статус аккаунтов
5. Имейте запасные аккаунты

## 🧪 Тестирование

```bash
# Проверка Python сервиса
cd backend/python-service
python -c "import config; print('Config OK')"

# Тест подключения к БД
python -c "from supabase_client import SupabaseClient; import asyncio; asyncio.run(SupabaseClient().connect())"

# Проверка Backend API
curl http://localhost:3000/api/messaging/accounts \
  -H "x-user-id: 00000000-0000-0000-0000-000000000001"
```

## 📝 TODO / Будущие улучшения

- [ ] Автоматическая конвертация tdata через UI
- [ ] Webhook для быстрых ответов (вместо polling)
- [ ] Расширенная аналитика диалогов
- [ ] A/B тестирование промптов
- [ ] Интеграция с CRM системами
- [ ] Автоматическое определение оптимальных лимитов
- [ ] ML для предсказания вероятности ответа
- [ ] Поддержка групповых чатов

## 🆘 Troubleshooting

### Ошибка: "Cannot connect to database"
- Проверьте `SUPABASE_DB_PASSWORD` в `.env`
- Убедитесь что Supabase project активен
- Проверьте сетевое подключение

### Ошибка: "Account not authorized"
- Session файл некорректный или устарел
- Пересоздайте session через Telethon
- Проверьте api_id и api_hash

### FloodWait слишком часто
- Уменьшите `MAX_MESSAGES_PER_DAY`
- Увеличьте `MESSAGE_DELAY_MIN` и `MESSAGE_DELAY_MAX`
- Добавьте больше аккаунтов для ротации

### AI не детектирует горячие лиды
- Уточните критерии в кампании
- Проверьте `OPENROUTER_API_KEY`
- Посмотрите логи Python сервиса

## 📚 Документация

- [Python Service README](backend/python-service/README.md)
- [API Documentation](backend/src/api/routes/messaging.js)
- [Frontend Components](frontend/src/pages/AIMessaging.jsx)

## 🎉 Готово!

Система полностью функциональна и готова к использованию.
Следуйте инструкциям выше для настройки и запуска.

**Удачи в автоматизации работы с лидами!** 🚀

