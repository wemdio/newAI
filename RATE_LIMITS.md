# ⚡ Rate Limiting Configuration

## 📊 Текущие лимиты (оптимизировано для multi-tenant SaaS)

### 🟢 Light Limiter (для частых запросов)
**1000 запросов / 15 минут** на пользователя

**Применяется к:**
- `GET /api/scanner/status` - статус сканера (polling каждые 10 секунд)

**Почему высокий лимит:**
- Frontend опрашивает статус каждые 10 секунд
- За 15 минут = 90 запросов от одного пользователя
- Запас на 10+ одновременных пользователей

---

### 🟡 General Limiter (для обычных запросов)
**500 запросов / 15 минут** на пользователя

**Применяется к:**
- `GET /api/config` - получение конфигурации
- `GET /api/leads` - получение лидов
- `PUT /api/config` - обновление конфигурации
- `PUT /api/leads/:id` - обновление лида
- `DELETE /api/leads/:id` - удаление лида
- И другие обычные API эндпоинты

**Почему достаточно:**
- Обычный пользователь делает 10-20 запросов в минуту
- 500 запросов / 15 минут = ~33 запроса в минуту
- Покрывает активное использование + запас

---

### 🔴 Strict Limiter (для дорогих операций)
**50 запросов / час** на пользователя

**Применяется к:**
- `POST /api/scanner/start` - запуск сканера
- `POST /api/scanner/stop` - остановка сканера
- `POST /api/scanner/manual-scan` - ручное сканирование
- Другие дорогие операции (AI анализ, массовые операции)

**Почему строгий:**
- Операции с высокой нагрузкой на систему
- Предотвращение злоупотребления
- 50 запусков/остановок в час - более чем достаточно

---

## 🔑 Ключ лимитирования

**По умолчанию:** User ID из заголовка `x-user-id`  
**Fallback:** IP адрес (если User ID не указан)

```javascript
keyGenerator: (req) => {
  return req.headers['x-user-id'] || req.ip;
}
```

**Почему по User ID:**
- Multi-tenant SaaS - много пользователей могут быть за одним IP
- Офисы, VPN, прокси - все пользователи имели бы один лимит на IP
- User ID = справедливое распределение между клиентами

---

## 📈 Масштабирование

### При росте количества пользователей:

**Если возникают проблемы:**
1. Увеличьте `max` в `generalLimiter` (500 → 1000)
2. Увеличьте интервал polling в frontend (10 сек → 15 сек)
3. Рассмотрите переход на WebSockets вместо polling

**Если слишком много запросов к базе:**
1. Добавьте кеширование для `GET /api/config`
2. Добавьте кеширование для `GET /api/scanner/status`
3. Используйте Redis для rate limiting (вместо in-memory)

---

## 🛠️ Настройка лимитов

### Frontend polling интервалы:

**Configuration.jsx:**
```javascript
// Статус сканера - каждые 10 секунд
const interval = setInterval(loadScannerStatus, 10000);
```

**Рекомендации:**
- 10 секунд - оптимально для UX
- Меньше 5 секунд - излишне, увеличивает нагрузку
- Больше 15 секунд - пользователь не сразу видит изменения

---

### Backend rate limiters:

**Изменить лимиты** в `backend/src/api/middleware/rateLimiter.js`:

```javascript
// Увеличить общий лимит
export const generalLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 1000, // Было 500
  // ...
});

// Увеличить лимит для polling
export const lightLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 2000, // Было 1000
  // ...
});
```

---

## 📊 Мониторинг

### Логи rate limit превышений:

В логах ищите:
```
"Rate limit exceeded" - общий лимит
"Strict rate limit exceeded" - строгий лимит
"Light rate limit exceeded" - лимит polling
```

**Формат логов:**
```json
{
  "level": "warn",
  "message": "Rate limit exceeded",
  "userId": "uuid-here",
  "ip": "192.168.1.1",
  "path": "/api/scanner/status"
}
```

---

## 🚨 Что делать при 429 ошибке

### Для пользователя:
1. Подождите 1-2 минуты
2. Обновите страницу (F5)
3. Если повторяется - свяжитесь с поддержкой

### Для администратора:
1. Проверьте логи - какой эндпоинт превышен
2. Если `scanner/status` - увеличьте `lightLimiter.max`
3. Если общие запросы - увеличьте `generalLimiter.max`
4. Если злоупотребление - заблокируйте User ID

---

## 🔒 Безопасность

### Защита от:
- **DDoS:** Лимиты на IP и User ID
- **Brute force:** Отдельный `authLimiter` (5 попыток/15мин)
- **API abuse:** Строгие лимиты на дорогие операции
- **Multi-account:** Лимиты по User ID, не обходятся новыми аккаунтами

### НЕ защищено:
- Распределенные атаки с разных IP (нужен WAF)
- Продвинутые боты (нужен Cloudflare/reCAPTCHA)

---

## ✅ Рекомендации для продакшена

### Обязательно:
1. ✅ Используйте Redis для rate limiting (не in-memory)
2. ✅ Настройте мониторинг превышений лимитов
3. ✅ Добавьте алерты при частых 429 ошибках
4. ✅ Периодически проверяйте логи на аномалии

### Желательно:
- Добавить Cloudflare перед приложением
- Настроить auto-scaling при высокой нагрузке
- Кешировать частые запросы (Redis/Memcached)
- Использовать WebSockets для real-time данных

---

## 📝 Примеры кода

### Применить другой лимит к маршруту:

```javascript
import { lightLimiter } from '../middleware/rateLimiter.js';

// Вместо общего лимита, использовать легкий
router.get('/status', lightLimiter, async (req, res) => {
  // ...
});
```

### Создать свой лимит:

```javascript
export const customLimiter = rateLimit({
  windowMs: 60 * 1000, // 1 минута
  max: 100, // 100 запросов
  keyGenerator: (req) => req.headers['x-user-id'] || req.ip,
  message: { error: 'Custom rate limit' }
});
```

---

## 🎯 Итого

**Текущая конфигурация:**
- ✅ Оптимизирована для multi-tenant SaaS
- ✅ Лимиты по User ID (не IP)
- ✅ Разные лимиты для разных типов запросов
- ✅ Polling каждые 10 секунд (оптимально)
- ✅ Запас на 10+ одновременных пользователей

**Следующие шаги при масштабировании:**
1. Redis для rate limiting
2. Кеширование config/status
3. WebSockets вместо polling
4. Load balancer + multiple instances

