# 🚀 Инструкция по деплою Frontend на Timeweb Cloud

## ✅ Исправления применены в ветке `frontend-deploy`

### 🔧 Что было исправлено:

1. **Убрали TypeScript из сборки** - `package.json` теперь использует только `vite build`
2. **Добавили nginx конфигурацию для SPA** - роутинг React Router теперь работает правильно
3. **Обновили backend URL** - `https://wemdio-newai-f239.twc1.net/api`
4. **Supabase credentials уже настроены** - есть fallback значения в коде

---

## 📋 Настройка на Timeweb Cloud

### Вариант 1: Обновить существующее приложение

1. **Откройте Timeweb Cloud Dashboard**
2. Перейдите в **Приложения** → **wemdio newai frontend v6**
3. В настройках приложения:
   - **Ветка**: измените на `frontend-deploy` ⚠️ **ВАЖНО!**
   - **Dockerfile**: `Dockerfile` (в корне репозитория)
   - **Тип**: Frontend
   - **Фреймворк**: Docker
   - **Порт**: `80`
4. Сохраните настройки
5. Нажмите **"Пересобрать"** или **"Redeploy"**

### Вариант 2: Создать новое приложение

1. **Timeweb Cloud Dashboard** → **Приложения** → **Создать**
2. Настройки:
   - **Название**: `newai-frontend`
   - **Тип**: Frontend
   - **Фреймворк**: Docker
   - **VCS провайдер**: GitHub (подключите репозиторий)
   - **Репозиторий**: `wemdio/newAI`
   - **Ветка**: `frontend-deploy` ⚠️
   - **Dockerfile**: `Dockerfile`
   - **Порт**: `80`
3. **Переменные окружения** (опционально, уже есть fallback):
   ```env
   VITE_SUPABASE_URL=https://liavhyhyzqadilfmicba.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxpYXZoeWh5enFhZGlsZm1pY2JhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjE1ODQ1NzIsImV4cCI6MjA3NzE2MDU3Mn0.tlqzG7LygCEKPtFIiXxChqef4JNMaXqj69ygLww1GQM
   VITE_API_URL=https://wemdio-newai-f239.twc1.net/api
   ```
4. Запустите деплой

---

## 🔍 Проверка деплоя

### В Timeweb Cloud

После завершения сборки (3-5 минут):

1. **Статус**: должен быть "Запущен" (зеленый)
2. **Кнопка "Открыть"**: должна стать активной ✅
3. **Логи** должны показать:
   ```
   ✓ built in XXXXms
   nginx: configuration is correct
   nginx: [notice] start worker processes
   ```

### В браузере

1. Откройте URL вашего приложения
2. Должна открыться **страница входа** (Login)
3. Откройте консоль (F12) - не должно быть красных ошибок
4. Попробуйте войти:
   - Email: ваш email из Supabase
   - Password: ваш пароль

---

## ⚠️ Важно!

### Структура проекта в ветке `frontend-deploy`

В этой ветке файлы frontend находятся **в корне репозитория**:
```
/
├── Dockerfile          ← Frontend Dockerfile
├── package.json        ← Frontend зависимости
├── index.html
├── vite.config.js
└── src/
    ├── App.jsx
    ├── main.jsx
    └── ...
```

### Разница с веткой `worker-deploy`

- `worker-deploy` - файлы в папке `frontend/`
- `frontend-deploy` - файлы в корне репозитория

**Поэтому важно использовать ветку `frontend-deploy`!**

---

## 🐛 Troubleshooting

### "Build failed: Command 'tsc' not found"
✅ **Исправлено!** - TypeScript убран из команды сборки

### "404 Not Found" при переходе по страницам
✅ **Исправлено!** - nginx теперь настроен для SPA

### "Failed to fetch" при запросах к API
**Решение:** Проверьте что backend запущен по адресу:
`https://wemdio-newai-f239.twc1.net`

### Кнопка "Открыть" все еще не активна
**Решение:**
1. Проверьте логи сборки - найдите ошибку
2. Убедитесь что используете ветку `frontend-deploy`
3. Попробуйте пересобрать приложение

---

## 📊 Текущее состояние

✅ **Commit**: `aad6a05d`  
✅ **Branch**: `frontend-deploy`  
✅ **Push**: Да (в GitHub)  
✅ **Готово к деплою**: Да

### Файлы изменены:
- `Dockerfile` - добавлена nginx конфигурация для SPA
- `package.json` - убран TypeScript из build команды
- `src/services/api.js` - обновлен backend URL

---

## 🎉 После успешного деплоя

1. Откройте фронтенд по URL из Timeweb Cloud
2. Войдите в систему (email/password)
3. Настройте систему:
   - Перейдите в **Configuration**
   - Добавьте OpenRouter API Key
   - Добавьте Telegram Channel ID
   - Настройте критерии поиска лидов
4. Начните использовать! 🚀

---

**Создано:** 20 ноября 2025  
**Ветка:** frontend-deploy  
**Commit:** aad6a05d

